#include <iostream>

int main()
{
	// Init socket
	WSADATA wsData;

	// Create a socket

	// Bind socket to an ip address and port

	// Tell sock it socket is for listening

	// Wait for connection

	// Close listening socket

	// While loop: accept and echo message back to the client

	// Close the sock

	// Shutdown the sock
}
